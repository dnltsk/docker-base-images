#!/bin/bash
source bashspec.sh

it_should_run_correct_php_version() {
  assert~ "$(php --version 2>&1)" "PHP 5.6."
}

it_should_have_correct_composer_version() {
  assert~ "$(composer --version)" "version 1."
}

run_tests
