#!/bin/bash
source ./bashspec.sh

it_should_run_correct_php_version() {
  assert_re "$(php --version 2>&1)" "PHP 5.6."
}

it_should_have_correct_composer_version() {
  assert_re "$(composer --version)" "version 1."
}

it_should_have_haarp_installed() {
  haarp
  assert $? 0
}


run_tests
